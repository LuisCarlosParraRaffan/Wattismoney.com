// Servicio de persistencia (localStorage)
// Se implementará la lógica cuando definamos los datos de cada pantalla.

export class StorageService {
  static clear(): void {
    localStorage.clear();
  }
}
